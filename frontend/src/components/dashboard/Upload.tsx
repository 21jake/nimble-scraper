import React from 'react';

interface IUploadProps {}

const Upload = ({}: IUploadProps ) => {
        return (
            <>
                 Upload
            </>
        );
}

export default Upload